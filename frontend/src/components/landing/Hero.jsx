import React from 'react'

function Hero() {
  return (
    // Add the 'id' here, matching the 'to' value in navItems
    <div id="hero" className="pt-20 ..."> 
      Hero Section
    </div>
  );
}

export default Hero;